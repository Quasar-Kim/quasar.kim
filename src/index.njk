<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/classless.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
    <title>Quasar.Kim</title>
    <style>
        .post-card {
            display: block;
            padding: 1rem;
            margin: 1rem 0;
        }

        .post-card h4 {
            margin-top: 0.6rem;
        }


    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Quasar.Kim</a></li>
                <li class="float-right sticky">
                    <a href="https://github.com/Quasar-Kim" target="_blank">
                        <img src="/static/icon/mark-github.svg" alt="github icon" class="icon dim">
                    </a>
                </li>
                <li class="float-right sticky">
                    <a href="/feed.xml" target="_blank">
                        <img src="/static/icon/rss.svg" alt="rss icon" class="icon dim">
                    </a>
                </li>
            </ul>
        </nav>
    </header>
    <main>
            {% for post in collections.blog | reverse %}
                <div class="bg-light post-card">
                    <a href="{{ post.url }}"><h4>{{ post.data.title }}</h4></a>
                    <p>{{ post.data.summary }}</p>
                    <time>{{ post.date.toLocaleDateString('ko-KR') }}</time>
                </div>
            {% endfor %}
        </div>
    </main>
    
</body>
</html>